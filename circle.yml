general:
  branches:
    ignore:
      - /^ultron\/.*/ # Ignore ultron/* branches

machine:
  node:
    version: 6.13.1

test:
  override:
    - npm run --silent lint -- --max-warnings 0
    - npm test
